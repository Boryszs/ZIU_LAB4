# When running `npm run build`, Create React App will use this value to prefix
# all asset paths, which is necessary for deploying to a sub-path on GitHub Pages.
# In development (`npm start`), this file is ignored, and PUBLIC_URL defaults to "".
PUBLIC_URL=/ZIU_LAB4